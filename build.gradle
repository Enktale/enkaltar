plugins {
    id 'java'
}

group = 'com.socratiemes'
version = '1.0.0'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    compileOnly files('D:/GameFiles/Hytale/Gamefiles/install/release/package/game/latest/Server/HytaleServer.jar')
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}